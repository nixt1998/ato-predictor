# ATO CardiTox 预测系统 - 快速启动指南

## 📦 一键启动脚本

### 🚀 启动所有服务
双击运行：
```
START_ALL.bat
```

**功能说明：**
- ✅ 自动检查 R 和 Node.js 环境
- ✅ 验证模型文件完整性
- ✅ 后台启动 R API 服务（端口 8000）
- ✅ 前台启动 Next.js 前端（端口 3000）

**访问地址：**
- 前端界面：http://localhost:3000
- R API 文档：http://localhost:8000/__docs__/

---

### 🛑 停止所有服务
双击运行：
```
STOP_ALL.bat
```

**功能说明：**
- 强制停止所有 Rscript.exe 进程
- 强制停止所有 node.exe 进程
- 检查端口占用情况

---

## 🔧 单独启动（高级）

### 仅启动 R API
```
cd r-api
双击 start_api.bat
```

### 仅启动前端
```
双击 start_frontend.bat
```

---

## 📋 系统要求

### 必需环境
- **R 4.5.0**（默认路径：`C:\Program Files\R\R-4.5.0\bin\x64\Rscript.exe`）
- **Node.js**（需在 PATH 环境变量中）

### R 依赖包
```r
install.packages(c("plumber", "tidymodels", "xgboost", "jsonlite"))
```

### Node.js 依赖
```bash
npm install
```

---

## 🐛 常见问题

### 1. R API 启动失败
**症状：** 提示 "未找到 R 4.5.0"

**解决方案：**
- 确认 R 安装在默认路径
- 或修改 `START_ALL.bat` 中的 `RSCRIPT_PATH` 变量

---

### 2. 端口被占用
**症状：** 提示端口 8000 或 3000 已被使用

**解决方案：**
```bash
# 查看端口占用
netstat -ano | findstr ":8000"
netstat -ano | findstr ":3000"

# 或直接运行
STOP_ALL.bat
```

---

### 3. 模型文件缺失
**症状：** 启动时提示缺少 .rds 文件

**解决方案：**
- 确认 `r-api/` 目录下存在以下文件：
  - `optim_wflow_last_fit.rds`
  - `non_select_features_data.rds`
  - `train_data.rds`

---

## 📁 目录结构

```
ato-predictor/
├── START_ALL.bat           ← 一键启动所有服务
├── STOP_ALL.bat            ← 停止所有服务
├── start_frontend.bat      ← 单独启动前端
├── r-api/
│   ├── start_api.bat       ← 单独启动 R API
│   ├── start.R             ← R API 启动脚本
│   ├── api.R               ← R API 主逻辑
│   └── *.rds               ← 模型文件
├── app/                    ← Next.js 页面
├── components/             ← React 组件
└── lib/                    ← 工具函数
```

---

## 🔍 健康检查

启动后，可通过以下方式验证服务状态：

### R API 健康检查
```bash
curl http://localhost:8000/health
# 预期返回: {"status":"ok","timestamp":"..."}
```

### 前端访问
浏览器打开：http://localhost:3000

---

## 📝 开发提示

### 热重载
- **R API**：修改 `api.R` 后需重启服务
- **前端**：Next.js 自动热重载，无需重启

### 日志查看
- **R API**：查看 `ATO R-API` 窗口
- **前端**：查看主命令行窗口

---

## 🎯 快速测试

启动后，可以发送测试请求：

```bash
curl -X POST http://localhost:8000/predict ^
  -H "Content-Type: application/json" ^
  -d "{\"iAs\":50,\"MMA\":100,\"DMA\":100,\"CT_drug\":\"Yes\"}"
```

预期返回完整预测结果（包含风险等级、代谢指标、SHAP值等）。

---

**版本：** v2.10.3  
**最后更新：** 2026-08-20
